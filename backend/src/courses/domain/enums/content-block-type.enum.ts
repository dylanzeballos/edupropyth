export enum ContentBlockType {
  VIDEO = 'video',
  DOCUMENT = 'document',
  RESOURCE = 'resource',
  HTML = 'html',
  ACTIVITIES = 'activities',
}

export enum LayoutType {
  FULL = 'full',
  HALF = 'half',
  THIRD = 'third',
  TWO_THIRDS = 'two-thirds',
}
